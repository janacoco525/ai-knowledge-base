# AI 知识库 —— 长文档处理原则

> 统一全系统的**长文档（>10k 字）** LLM 处理规范，所有涉及长文本的功能（脑图、实体抽取、总结、卡片、对比等）**必须遵循本文档**。

---

## 1. 核心原则

| 原则 | 说明 |
|------|------|
| **分而治之** | 超长文本必须切片 → 并行/串行处理 → 归并，严禁直接塞给 LLM |
| **有损压缩优先** | 先摘要/抽章节 → 再细化，杜绝全量全文喂给 LLM |
| **结构优先** | 先拿文档结构（目录/章节/标题层级），再按结构分块处理 |
| **增量迭代** | 粗粒度 → 细粒度分级生成，每层可独立验证 |
| **缓存复用** | 同一文档+同任务缓存结果，避免重复调用 LLM |
| **可观测性** | 每步记录耗时、token、错误，便于调优与审计 |
| **兜底降级** | 超时/报错 → 返回已有结果/结构化降级，不让用户看到报错 |

---

## 2. 分层处理流程（标准化）

```
原始文档 (N 字)
    │
    ├─ 1️⃣ 结构提取 ──→ 目录/标题层级/章节边界
    │
    ├─ 2️⃣ 粗略摘要 ──→ 每章 1 段摘要 (≤500 字) → 章节摘要索引
    │
    ├─ 3️⃣ 任务分发 ──→ 按任务类型分发到专用 Prompt
    │   ├─ 脑图 → 按章节生成子树 → 合并
    │   ├─ 实体抽取 → 并行处理每段 → 去重合并
    │   ├─ 总结 → 摘要合并 → 最终润色
    │   └─ 卡片/对比 → 关键点提取 → 模板填充
    │
    └─ 4️⃣ 结果归并 ──→ 统一格式化 → 缓存 → 返回
```

---

## 3. 切片策略（通用）

| 维度 | 规则 |
|------|------|
| **最大切片** | 8000 字（留 2000 token 给 prompt+output） |
| **重叠窗口** | 200-500 字（保证上下文连贯） |
| **最大并行度** | 6 个并发（避免限流） |
| **章节优先** | 有结构按章节切，无结构按固定长度+重叠 |
| **最大分片数** | 单任务 ≤ 12 片（超出则先摘要再处理） |

---

## 4. 功能级处理规范

### 4.1 脑图生成
- **≤8k**：单次生成完整树
- **8k-50k**：章节优先 → 每章生成子树 → 合并（并发 ≤6）
- **>50k**：先摘要全文 → 生成骨架 → 再按需细化（懒加载）
- **节点限制**：根≤1，二级≤12，三级≤每父5，四级仅关键证据
- **缓存键**：`fileId + content_hash[:16] + "mindmap"`

### 4.2 实体/关系抽取
- **分块并行** → 各块抽取 → 去重合并（按 ID/标签）
- **小说模式**：人物/事件/关系分离提取
- **知识模式**：概念/实体/关系/属性四元组
- **去重键**：`(type, normalized_name)`

### 4.3 总结/摘要
- **Map-Reduce**：Map(分块摘要) → Reduce(合并润色)
- **层级摘要**：文档级 → 章节级 → 段落级
- **关键点保留**：数字/专有名词/因果链必须保留

### 4.4 知识卡片
- **单卡 ≤ 300 字**
- **关键点提取 → 模板填充**
- **来源追溯**：每卡标注来源章节/位置

---

## 5. Prompt 工程规范

| 项 | 要求 |
|----|------|
| **系统提示** | 固定角色 + 任务目标 + 输出格式 + 质量标准 |
| **Few-shot** | ≥3 个高质量示例（含正负例） |
| **格式约束** | 显式 JSON Schema / Markdown 结构 |
| **Token 预算** | `max_tokens = min(剩余预算, 任务上限)`，留 20% 余量 |
| **Temperature** | 结构化任务 0.1-0.3；创意类 0.5-0.7 |
| **超时** | 单次 ≤ 60s（脑图 180s） |

---

## 6. 错误处理与降级

| 场景 | 策略 |
|------|------|
| LLM 超时 | 返回已完成部分 + `"partial": true` |
| JSON 解析失败 | 正则兜底 → 再次请求修正 → 仍失败返回结构化错误 |
| Token 超限 | 自动缩减输入/切片重试 |
| 网络/限流 | 指数退避重试 3 次 → 返回缓存/降级结果 |
| 内容为空 | 返回结构化空对象 `{topic: title, children: []}` |

---

## 7. 缓存策略

| 层级 | Key 设计 | TTL | 失效条件 |
|------|----------|-----|----------|
| **文档级** | `fileId:md5(content)[:16]:task` | 7 天 | 文件内容变更 |
| **章节级** | `fileId:chapter_idx:task` | 7 天 | 章节内容变更 |
| **摘要级** | `fileId:summary:v1` | 30 天 | 手动清理 |

> **缓存优先**：所有 LLM 调用前先查缓存，命中直接返回 `cached: true`

---

## 8. 可观测性指标（每次调用必记录）

```json
{
  "task": "mindmap|extract|summarize|...",
  "file_id": "...",
  "input_chars": 12345,
  "chunks": 3,
  "tokens_in": 4500,
  "tokens_out": 1200,
  "latency_ms": 12400,
  "cached": false,
  "status": "success|partial|error",
  "error": "timeout|parse_failed|..."
}
```

---

## 9. 代码约定（Python）

```python
# 统一入口：llm_client_factory.token_budget() 计算可用 token
# 统一切片：utils.chunk_text(text, max_len=8000, overlap=200)
# 统一缓存：eng.kb.get_llm_cache(key, task) / set_llm_cache(key, task, val)
# 统一日志：logger.info("llm.call", extra={...metrics...})
```

---

## 10. 迁移清单（存量代码逐项对齐）

- [ ] `llm_ops.py` 脑图：按章节分块+并发+缓存
- [ ] `llm_graph_extractor.py`：分块并行+去重合并
- [ ] `summarize`：Map-Reduce 两阶段
- [ ] `cards`：关键点提取+模板
- [ ] 统一切片工具 `utils/chunk_text.py`
- [ ] 统一缓存键命名 `fileId:hash:task`
- [ ] 统一指标上报 `logger.info("llm.call", extra={...})`
- [ ] 单测覆盖：切片/缓存/归并/降级

---

> **执行原则**：新增功能先写测试用例（含长文本边界），再实现。所有 PR 必须通过 `pytest -xvs tests/test_long_doc.py` 方可合并。